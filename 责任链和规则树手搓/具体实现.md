## 两个接口

```java
public interface StrategyHandler<T, D, R> {

    StrategyHandler DEFAULT = (T, D) -> null;

    R apply(T requestParameter, D dynamicContext) throws Exception;

}
```

这个接口是用来处理业务逻辑的接口

```java
package cn.bugstack.wrench.design.framework.tree;

/**
 * @author Fuzhengwei bugstack.cn @小傅哥
 * @description 策略映射器
 * T 入参类型
 * D 上下文参数
 * R 返参类型
 * @create 2024-12-14 12:05
 */
public interface StrategyMapper<T, D, R> {

    /**
     * 获取待执行策略
     *
     * @param requestParameter 入参
     * @param dynamicContext   上下文
     * @return 返参
     * @throws Exception 异常
     */
    StrategyHandler<T, D, R> get(T requestParameter, D dynamicContext) throws Exception;

}

```

这个接口是用来传递节点的，用于指路，返回下一个需要执行的执行器 也就是上面那个接口

注：T D R 分别代表 入参类型 上下文类型 返回类型

下一步是整合了两个接口的AbstractStrategyRouter类

```java
public abstract class AbstractStrategyRouter<T, D, R> implements StrategyMapper<T, D, R>, StrategyHandler<T, D, R> {

    @Getter
    @Setter
    protected StrategyHandler<T, D, R> defaultStrategyHandler = StrategyHandler.DEFAULT;

    public R router(T requestParameter, D dynamicContext) throws Exception {
        StrategyHandler<T, D, R> strategyHandler = get(requestParameter, dynamicContext);
        if(null != strategyHandler) return strategyHandler.apply(requestParameter, dynamicContext);
        return defaultStrategyHandler.apply(requestParameter, dynamicContext);
    }

}
```

这是路由抽象类：

组合两个基策略接口 实现了router方法

router方法就是根据入参数 去上下文中找对应的mapper实现类 并返回

注：接口或者抽象类中 声明常量 如图

## 多线程增强模版

```java
public abstract class AbstractMultiThreadStrategyRouter<T, D, R> implements StrategyMapper<T, D, R>, StrategyHandler<T, D, R> {

    @Getter
    @Setter
    protected StrategyHandler<T, D, R> defaultStrategyHandler = StrategyHandler.DEFAULT;

    public R router(T requestParameter, D dynamicContext) throws Exception {
        StrategyHandler<T, D, R> strategyHandler = get(requestParameter, dynamicContext);
        if(null != strategyHandler) return strategyHandler.apply(requestParameter, dynamicContext);
        return defaultStrategyHandler.apply(requestParameter, dynamicContext);
    }

    @Override
    public R apply(T requestParameter, D dynamicContext) throws Exception {
        // 异步加载数据
        multiThread(requestParameter, dynamicContext);
        // 业务流程受理
        return doApply(requestParameter, dynamicContext);
    }

    /**
     * 异步加载数据
     */
    protected abstract void multiThread(T requestParameter, D dynamicContext) throws ExecutionException, InterruptedException, TimeoutException;

    /**
     * 业务流程受理
     */
    protected abstract R doApply(T requestParameter, D dynamicContext) throws Exception;

}
```

就是实现了两个基础接口 的编排 和路由 



## 具体测试需要

需要一个工厂类，里面定义上下文对象（就是一个map） 并配对get set方法 然后还需要一个开启方法 返回rootnode节点 然后这个节点去apply 开启全流程

节点：

需要一个根节点

一些业务节点

节点必须要实现的 就是doapply接口 实现业务逻辑  然后返回下一个hanlder类的apply

其次就是get方法 来指路

support实现类一个空实现 这样就不用每个节点都实现多线程异步加载的接口 安需要实现

总结： 

上层：

两个方法：hadler mapper

handler 提供apply接口 mapper提供get接口

中层：

多线程抽象类：

编排了重写了 里面加上了前置异步加载方法

其中有router 串联方法 通过get拿到下一个节点（hanler实现类） 然后调用实现类的apply方法 继续下一步

知道终结
